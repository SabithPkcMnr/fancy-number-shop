import { pageMeta } from "@/lib/seo";

export const metadata = pageMeta(
  "Contact Fancy Number Shop, Mukkom Calicut",
  "Call 9747888999 or visit Fancy Number Shop in Mukkom, Kozhikode (Calicut), Kerala 673602. Buy one VIP number at a time on Razorpay or WhatsApp.",
  "/contact",
);

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
