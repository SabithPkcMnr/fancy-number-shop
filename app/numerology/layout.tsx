import { pageMeta } from "@/lib/seo";

export const metadata = pageMeta(
  "Numerology Mobile Number Search",
  "Search VIP fancy mobile numbers by destiny number, total, and lucky digits. Numerology catalogue from Fancy Number Shop, Mukkom, Calicut, Kerala.",
  "/numerology",
);

export default function NumerologyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
